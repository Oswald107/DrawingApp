DONE
connected line
color pick
size
not stretched - had to relearn a bit of linear algebra for this, it wasn't difficult I was just stubborn. Also I was stuck since I was using the wrong function
zoom
alternative pen, draws 2 triangles between points instead of filling with circles
undo
redo
save (simple save, I press a button and it saves the image to output.png)
fbo initial setup done
mouse movement
- scroll to zoom
- right click to pan
basic eraser done (pen with background color)
complex eraser (if we want to have multiple layers we need something that can show the layer beneath when erased)
- pen lines and erase lines now inherit from the Stroke class, all strokes are kept track of under the layer lines vector and drawn in order
press and hold undo/redo with decreasing delay
layer poc, 2 layers that can be swapped with 1 and 2
layers - each layer is an object with its own local space, higher layers are drawn over lower ones
- add layer
- visualize layer info
drawing background differnt from world background

TODO
moving sliders shouldn't draw
make circle not produce extras when held? (find a way to lock the alpha of the current stroke so overlapping itself doesn't matter)
make cursor circle

better save (pull up file explorer at least)
open (create a layer and place texture in the center)
rgb adjust hsv

layers - each layer is an object with its own local space, higher layers are drawn over lower ones
- move layer position in the world
- move layer heirarchy (probably better when I have a better way to visualize layers)
- better layer visualization
color picker